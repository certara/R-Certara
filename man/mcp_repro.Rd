% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp_repro.R
\name{mcp_repro_reset}
\alias{mcp_repro_reset}
\alias{mcp_repro}
\alias{mcp_repro_path}
\alias{mcp_repro_record}
\alias{mcp_repro_read}
\alias{mcp_repro_info}
\alias{mcp_repro_sym}
\alias{mcp_repro_call}
\title{Reproducible-script recorder}
\usage{
mcp_repro_reset()

mcp_repro_path(path = NULL)

mcp_repro_record(code, libraries = character())

mcp_repro_read()

mcp_repro_info()

mcp_repro_sym(x)

mcp_repro_call(fn, args = list(), var = NULL)
}
\arguments{
\item{path}{New script path (\code{mcp_repro_path}); setting it resets contents.}

\item{code}{Character vector of runnable R code (\code{mcp_repro_record}).}

\item{libraries}{Package names to attach once at the top of the script.}

\item{x}{Value to mark as a verbatim symbol (\code{mcp_repro_sym}).}

\item{fn}{Function name to render (\code{mcp_repro_call}).}

\item{args}{Named list of argument values to render. \code{NULL} values are
dropped; an unnamed element is rendered positionally.}

\item{var}{Optional variable name to assign the rendered call to.}
}
\value{
\code{mcp_repro_path} returns the active path; \code{mcp_repro_read} the script
text; \code{mcp_repro_info} a list of \code{path} + \code{contents}; \code{mcp_repro_call} a
code string; others are called for their side effect.
}
\description{
A host-owned, provider-agnostic recorder that accumulates the exact runnable
R code executed by MCP tools into a single \code{.R} script per server session.
Host tools and provider tools both append to it, so re-running the script
reproduces the MCP-driven analysis for QC / audit.
}
\examples{
mcp_repro_reset()
mcp_repro_record(mcp_repro_call("lm", list(formula = mcp_repro_sym("y ~ x")),
                                var = "fit"))
cat(mcp_repro_read())
}
