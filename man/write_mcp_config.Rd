% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp_config.R
\name{write_mcp_config}
\alias{write_mcp_config}
\title{Write MCP client configuration for the Certara server}
\usage{
write_mcp_config(
  client = c("cursor", "claude-code", "codex", "claude-desktop"),
  scope = c("project", "user", "local"),
  btw_groups = "docs",
  session_tools = FALSE,
  tool_profile = c("full", "core", "authoring", "execution", "diagnostics"),
  server_name = "certara-r",
  project_dir = ".",
  tool_allowlist = TRUE,
  run = FALSE,
  job_watch_wait_seconds = NULL,
  agent = NULL
)
}
\arguments{
\item{client}{One or more of \code{"cursor"}, \code{"claude-code"}, \code{"codex"},
\code{"claude-desktop"}. A bare call defaults to \code{"cursor"} only; pass a vector
to configure several. \code{"claude-desktop"} targets Claude Desktop and its
Cowork local-agent mode (user scope only).}

\item{scope}{One or more of \code{"project"}, \code{"user"}, \code{"local"}. A bare call
defaults to \code{"project"} only. \code{"local"} is Claude Code-only (a per-project
private entry in \verb{~/.claude.json}); it is skipped with a warning for
\code{"cursor"}, which has no local tier, and is irrelevant to \code{"codex"}.}

\item{btw_groups}{Which general-purpose \code{btw} R tool groups to expose to the
agent alongside the Certara tools; the chosen groups are baked into the
launch command. The default \code{c("docs", "pkg")} provides:
\itemize{
\item \code{"docs"} - read R documentation for any installed package (help
pages, help-topic listings, vignettes, release notes), so the agent
can look up function usage on demand.
\item \code{"pkg"} - R package development actions (document, check, test,
coverage, load-all).
}
Two groups enable working against a live session bridged with
\code{btw::btw_mcp_session()} (set \code{session_tools = TRUE} as well): \code{"env"}
inspects that session's objects (loaded data frames, fitted models) and
\code{"run"} exposes \code{btw_tool_run_r}, which executes R code in that session's
global environment - include \code{"run"} only when you want the agent to run
arbitrary code there. Other groups \code{btw} offers include \code{"files"},
\code{"git"}, \code{"github"}, \code{"ide"}, \code{"cran"}, \code{"web"}, and \code{"sessioninfo"}. Use
\code{"docs"} alone for the leanest, lowest-latency server, or \code{character(0)} to
expose only the Certara tools.}

\item{session_tools}{Whether to expose tools that bridge to a separate, live
R session you register with \code{btw::btw_mcp_session()} (for example your
interactive RStudio session), letting the agent inspect that session's
objects - loaded data frames, fitted models - and run code there. When
\code{FALSE} (the default) the server uses only its own R process, which starts
faster and avoids the session hop; enable it to work against your live
workspace. When \code{TRUE}, pair it with \code{btw_groups} \code{"env"} (inspect objects)
and/or \code{"run"} (run code via \code{btw_tool_run_r}), then run
\code{btw::btw_mcp_session()} in the session you want reached; the call prints a
post-setup checklist. \code{\link[=certara_session_status]{certara_session_status()}} reports the live wiring.}

\item{tool_profile}{Curated tool subset baked into the launch command, to
keep the tool list focused: \code{"full"} (default), \code{"core"}, \code{"authoring"},
\code{"execution"}, or \code{"diagnostics"}. See \code{\link[=launch_certara_mcp]{launch_certara_mcp()}}.}

\item{server_name}{MCP server key (default \code{"certara-r"}).}

\item{project_dir}{Project root for project-scope files.}

\item{tool_allowlist}{When \code{TRUE}, pre-authorize the Certara MCP tools so they
run without per-tool approval prompts, using each requested client's native
mechanism for every requested scope:
\itemize{
\item Cursor: merge \verb{server_name:*} into \code{permissions.json}
(\verb{<project>/.cursor/permissions.json} or \verb{~/.cursor/permissions.json});
takes effect under Cursor Run Mode.
\item Claude Code: merge \code{mcp__server_name} into \code{permissions$allow} of the
scope-appropriate settings file (\verb{<project>/.claude/settings.json} for
project, \verb{<project>/.claude/settings.local.json} for local,
\verb{~/.claude/settings.json} for user).
\item Codex: write a user-scope managed block into
\verb{~/.codex/config.toml} with this MCP server and Codex MCP approval
settings. Most Certara tools are approved, while privileged cleanup and
arbitrary-R job tools stay prompt-gated.
}
Default \code{TRUE}.}

\item{run}{For CLI-managed targets (Claude Code user/local scope), actually
execute the client CLI when it is on \code{PATH} instead of only printing the
command. Falls back to printing if the CLI is not found. Default \code{FALSE}.
Codex is configured by writing \verb{~/.codex/config.toml} directly (the only way
to set per-tool approvals), so \code{run} does not invoke the Codex CLI; the
equivalent \verb{codex mcp add} command is reported for reference.}

\item{job_watch_wait_seconds}{Optional override for the per-call server-side
job-watch budget (seconds) baked into the launch command, used by
\code{\link[Certara.RsNLME:wait_for_nlme_job]{wait_for_nlme_job()}}. When \code{NULL} (the default) each client gets a sensible
default: \code{45} for Cursor (which caps a single MCP tool call at ~60s and has
no timeout setting) and \code{600} for Claude Code and Codex (which allow
long-running tool calls). Supply a single number to override every selected
client; clamped to \verb{0..600}.}

\item{agent}{Alias for \code{client}, for users who think of these as agent
configuration targets. Use either \code{client} or \code{agent}, not both.}
}
\value{
Invisibly, a list describing actions taken (written paths, snippets).
}
\description{
Write MCP client configuration for the Certara server
}
\details{
For \code{"claude-code"}, the routing/behavior contract is delivered as a
\code{CLAUDE.md}-imported guidance doc (\code{.claude/certara-mcp-usage.md} for
project/local, \verb{~/.claude/certara-mcp-usage.md} for user), since Claude Code
does not read Cursor \code{.mdc} rules; \code{\link[=remove_mcp_config]{remove_mcp_config()}} removes it.

For \code{"claude-desktop"} (Claude Desktop and Cowork), the server is merged into
\code{claude_desktop_config.json} (user scope only). On Windows the MSIX
virtualized path Desktop actually reads is targeted when present. A companion
\code{certara-mcp-usage.md} is written beside that file for manual reference -
Desktop/Cowork do not auto-load it the way Claude Code imports \code{CLAUDE.md},
so call \code{certara_mcp_capabilities} at session start. There is no working
config-file tool allowlist for Cowork, so \code{tool_allowlist} only prints a note
(approve tools in the UI per session). Fully quit and relaunch Claude Desktop
after config changes.

For the leanest, lowest-latency Claude Code or Desktop server, drop the
package-development tools with \code{btw_groups = "docs"} (or \code{character(0)} for
Certara tools only).
}
\examples{
\dontrun{
# Cursor, project scope (the bare-call default).
write_mcp_config()

# Lean Claude Code server: Certara tools + R docs only, no pkg-dev tools.
write_mcp_config("claude-code", btw_groups = "docs")

# Claude Code at user scope, registering via the claude CLI when present.
write_mcp_config("claude-code", scope = "user", run = TRUE)

# Claude Desktop / Cowork (merges claude_desktop_config.json).
write_mcp_config("claude-desktop")
}
}
