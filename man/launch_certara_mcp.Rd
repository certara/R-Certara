% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp_server.R
\name{launch_certara_mcp}
\alias{launch_certara_mcp}
\title{Launch the Certara MCP server}
\usage{
launch_certara_mcp(
  btw_groups = "docs",
  session_tools = FALSE,
  server_name = "certara-r",
  job_watch_wait_seconds = 45,
  dev_roots = character(0),
  providers = NULL,
  tool_profile = c("full", "core", "authoring", "execution", "diagnostics")
)
}
\arguments{
\item{btw_groups}{Which general-purpose \code{btw} R tool groups to expose
alongside the Certara tools (default \code{c("docs", "pkg")}). \code{"docs"} lets the
agent read R documentation; \code{"pkg"} adds package-development actions. For a
live session bridged with \code{btw::btw_mcp_session()} (needs
\code{session_tools = TRUE}), \code{"env"} exposes read-only object inspection and
\code{"run"} exposes \code{btw_tool_run_r}. Other groups: \code{"files"}, \code{"git"},
\code{"github"}, \code{"ide"}, \code{"cran"}, \code{"web"}, \code{"sessioninfo"}. Use \code{character(0)}
for none. Call \code{\link[=certara_session_status]{certara_session_status()}} to see which are active.}

\item{session_tools}{Whether to expose tools that bridge to a separate, live
R session registered with \code{btw::btw_mcp_session()}. Default \code{FALSE} uses
only the server's own R process.}

\item{server_name}{MCP config key this process serves (default \code{"certara-r"}).
Advertised in \code{\link[=certara_mcp_capabilities]{certara_mcp_capabilities()}} and used to build Cursor
\code{CallMcpTool} routing (\verb{user-<server_name>}).}

\item{job_watch_wait_seconds}{Per-call server-side watch budget (seconds) for
provider long-job watch tools. Default \code{45} keeps a single call under
Cursor's ~60s timeout; \code{\link[=write_mcp_config]{write_mcp_config()}} bakes a larger value (\code{600})
into the Claude Code and Codex launch commands. Clamped to \verb{0..600}.}

\item{dev_roots}{Optional dev source-tree roots for KB and tool-provider
discovery (unpublished provider trees).}

\item{providers}{Optional character vector of provider package names to
include (default: every discovered provider).}

\item{tool_profile}{Which curated tool subset to expose, to keep the tool
list focused: \code{"full"} (default, every tool), \code{"core"}
(authoring/validation + data inspection, no job launches), \code{"authoring"}
(core + model comparison), \code{"execution"} (core + fit/job tools), or
\code{"diagnostics"} (core + execution + comparison + interpretation).
\code{\link[=find_certara_tools]{find_certara_tools()}}, \code{\link[=certara_mcp_capabilities]{certara_mcp_capabilities()}}, and
\code{\link[=certara_session_status]{certara_session_status()}} are present in every profile.}
}
\value{
Does not return under normal operation (serves until the client
disconnects). Must be run non-interactively (the \code{Rscript -e} command an MCP
client launches); calling it from an interactive R session is an error.
}
\description{
Starts an stdio MCP server exposing curated btw tools, the host's built-in
Certara knowledge / memory / meta tools, and the tools contributed by every
discovered provider package (e.g. Certara.RsNLME). Intended to be the
\code{command} an MCP client runs (see \code{\link[=write_mcp_config]{write_mcp_config()}}). Blocks serving
requests.
}
\examples{
\dontrun{
# You do not call this in an interactive R session. Configure a client, which
# then launches the server as a non-interactive command:
write_mcp_config("cursor")

# The client runs the equivalent of:
#   Rscript -e 'Certara.R::launch_certara_mcp(tool_profile = "core")'
}
}
\keyword{internal}
