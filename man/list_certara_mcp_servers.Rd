% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp_registry.R
\name{list_certara_mcp_servers}
\alias{list_certara_mcp_servers}
\title{List running Certara MCP server processes}
\usage{
list_certara_mcp_servers(include_stale = FALSE)
}
\arguments{
\item{include_stale}{Logical; also include registry entries whose process
is no longer running (reported with \code{status = "stale"}). Default \code{FALSE}.}
}
\value{
A data frame with one row per discovered server: \code{server_name},
\code{pid}, \code{status} (\code{"running"} or \code{"stale"}), \code{started_at},
\code{package_version}, \code{r_version}, \code{tool_profile}, \code{session_tools},
\code{btw_groups}, \code{job_watch_wait_seconds}, and \code{working_dir}. Zero rows when
no server is running.
}
\description{
Reports Certara MCP server processes started with \code{\link[=launch_certara_mcp]{launch_certara_mcp()}} on
this machine, discovered from a per-user runtime registry written at server
startup - not by scanning system processes, which cannot reliably identify a
generic \code{Rscript} process as a Certara MCP server. Complements
\code{\link[=list_certara_mcp_configs]{list_certara_mcp_configs()}}, which reports what is \emph{configured} to launch;
this reports what has actually \emph{started}.
}
\details{
Each registry entry is verified against the live process (PID and process
start time) before being reported as \code{"running"}; entries whose process has
exited, or whose PID was since reused by an unrelated process, are treated
as stale and omitted unless \code{include_stale = TRUE}. Stale entries left by a
crashed or forcibly killed server are cleaned up automatically the next time
a Certara MCP server starts.
}
\examples{
list_certara_mcp_servers()
}
\seealso{
\code{\link[=list_certara_mcp_configs]{list_certara_mcp_configs()}}, \code{\link[=certara_session_status]{certara_session_status()}},
\code{\link[=launch_certara_mcp]{launch_certara_mcp()}}
}
