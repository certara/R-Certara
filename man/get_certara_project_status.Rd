% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp_project_status.R
\name{get_certara_project_status}
\alias{get_certara_project_status}
\title{Merged cross-provider project status}
\usage{
get_certara_project_status(project_dir = ".", dev_roots = character(0))
}
\arguments{
\item{project_dir}{Project root (default \code{"."}).}

\item{dev_roots}{Optional dev source-tree roots for provider discovery
(see \code{\link[=launch_certara_mcp]{launch_certara_mcp()}}).}
}
\value{
A list with \code{project_dir}, \code{providers} (one entry per provider that
declares a \code{status_hook}: \code{package}, \code{status_hook}, and either \code{status}
or \code{error}), \code{next_gated_phase} (a best-effort hint from whichever
provider's status names one, or \code{NULL}), \code{repro_script}, and
\code{report_rmd} (this session's own accumulated artifacts).
}
\description{
Calls every discovered provider's optional \code{status_hook} (declared in its
\code{inst/mcp/tools/manifest.json}, see \code{\link[=.mcp_discover_tool_providers]{.mcp_discover_tool_providers()}}) with
\code{project_dir} and merges the results into one snapshot, so an agent can see
where a project stands across Certara.RDarwin searches,
Certara.RsNLME jobs/VPCs/sequential-LRT sessions, and any other provider
that ships a hook - without this host hardcoding any provider's own status
representation. A provider with no \code{status_hook} is absent from \code{providers}
rather than treated as an error. Nothing is written; provider-local
artifacts (Darwin run directories, LRT session/decision files, repro
scripts) remain the source of truth - this only surfaces the pointers each
provider's status already returns (e.g. \code{run_dir}, \code{session_dir},
\code{plan_path}, \code{decisions_path}).
}
\keyword{internal}
