% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp_tool_index.R
\name{find_certara_tools}
\alias{find_certara_tools}
\title{Discover the tools relevant to a task}
\usage{
find_certara_tools(task, limit = 5)
}
\arguments{
\item{task}{Free-text description of what you are trying to do.}

\item{limit}{Maximum tools to return; clamped to 3..10 (default 5).}
}
\value{
A list with \code{query}, ranked \code{tools} (\code{name}, \code{summary}, \code{provider},
\code{score}), and a \code{note}. Empty \code{tools} means broaden the task or call
\code{\link[=certara_mcp_capabilities]{certara_mcp_capabilities()}}.
}
\description{
Retrieval over the registered tool catalog: returns the few tools whose
name/description best match a free-text task, so the agent picks from a short
ranked list instead of the full set. Call this first when unsure which tool
fits; then call the chosen tool(s) directly.
}
\examples{
\dontrun{
find_certara_tools("fit a population model from a spec")
find_certara_tools("wait for a running fit to finish", limit = 3)
}
}
\keyword{internal}
