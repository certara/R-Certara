% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp_config_list.R
\name{list_certara_mcp_configs}
\alias{list_certara_mcp_configs}
\title{Inventory Certara MCP entries across client configuration files}
\usage{
list_certara_mcp_configs(
  client = c("cursor", "claude-code", "codex", "claude-desktop"),
  scope = c("project", "user", "local"),
  project_dir = ".",
  server_name = NULL
)
}
\arguments{
\item{client}{One or more of \code{"cursor"}, \code{"claude-code"}, \code{"codex"},
\code{"claude-desktop"}. Defaults to all four.}

\item{scope}{One or more of \code{"project"}, \code{"user"}, \code{"local"}. Defaults to
all three (ignored by clients that are not scoped, i.e. Claude Desktop
and Codex, which are always reported at \code{"user"} scope).}

\item{project_dir}{Project root to check for project-scope files.}

\item{server_name}{Optional MCP server key to look for (default \code{"certara-r"}
convention if \code{NULL}: every configured entry is scanned instead, so a
server configured under a different name is still discovered).}
}
\value{
A data frame with one row per client/scope combination checked (or
per matching entry, if more than one server invokes
\code{\link[=launch_certara_mcp]{launch_certara_mcp()}} in the same file): \code{client}, \code{scope}, \code{path},
\code{exists}, \code{configured}, \code{server_key}, \code{status} (\code{"configured"},
\code{"not_configured"}, \code{"parse_error"}, or \code{"unsupported"}), \code{command},
\code{server_name}, \code{btw_groups}, \code{session_tools}, \code{job_watch_wait_seconds},
\code{tool_profile}, and \code{note}.
}
\description{
Scans the on-disk MCP client configuration files \code{\link[=write_mcp_config]{write_mcp_config()}}
writes to and reports whether a Certara MCP server is configured in each,
without starting or contacting any process. Complements
\code{\link[=list_certara_mcp_servers]{list_certara_mcp_servers()}}, which reports what has actually \emph{started};
this reports what is \emph{configured} to launch.
}
\details{
A missing or unparsable config file never aborts the scan - each requested
client/scope combination gets its own row and \code{status}, so one broken file
does not hide the rest. Claude Code's \code{"user"}/\code{"local"} scopes are managed
through its CLI (\verb{~/.claude.json}), which this function does not read or
write; those combinations are reported with \code{status = "unsupported"}, as is
Cursor's nonexistent \code{"local"} scope. Claude Desktop and Codex are always
user-scoped regardless of \code{scope}.
}
\examples{
list_certara_mcp_configs(client = "cursor", project_dir = tempdir())
}
\seealso{
\code{\link[=list_certara_mcp_servers]{list_certara_mcp_servers()}}, \code{\link[=write_mcp_config]{write_mcp_config()}},
\code{\link[=remove_mcp_config]{remove_mcp_config()}}
}
