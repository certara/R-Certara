# List all per-user memory records

List all per-user memory records

## Usage

``` r
list_memory_records()
```

## Value

A list with `run_memory`, `lessons`, `preferences`, `gaps`, and
`enabled`.

## Examples

``` r
if (FALSE) { # \dontrun{
list_memory_records()
} # }
```
