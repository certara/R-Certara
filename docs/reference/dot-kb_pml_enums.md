# Controlled vocabularies surfaced by list_pml_enums()

These are the closed sets the agent should pick from instead of
guessing. Generated into `index/enums.json` and also served at runtime.

## Usage

``` r
.kb_pml_enums()
```

## Value

A named list of character vectors.
