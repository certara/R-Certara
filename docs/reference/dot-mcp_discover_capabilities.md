# Discover provider capability fragments

Discover provider capability fragments

## Usage

``` r
.mcp_discover_capabilities(dev_roots = character(0))
```

## Arguments

- dev_roots:

  Optional package source-tree roots to include.

## Value

A list with `fragments` (valid) and `skipped` (with a reason).
