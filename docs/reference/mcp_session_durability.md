# Durability of the current MCP audit trail

Durability of the current MCP audit trail

## Usage

``` r
mcp_session_durability()
```

## Value

A list with `durable` (logical), `project_dir`, `reason`, and when not
durable a remediation `next_action`.
