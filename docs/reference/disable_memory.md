# Disable per-user memory (does not delete existing records)

Disable per-user memory (does not delete existing records)

## Usage

``` r
disable_memory()
```

## Value

Invisibly the new config.

## Examples

``` r
if (FALSE) { # \dontrun{
disable_memory()
} # }
```
