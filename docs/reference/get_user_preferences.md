# Get active user preferences

Get active user preferences

## Usage

``` r
get_user_preferences(context = NULL)
```

## Arguments

- context:

  Optional scope tag; returns global plus matching preferences.

## Value

A list of active preference records.

## Examples

``` r
if (FALSE) { # \dontrun{
get_user_preferences()
} # }
```
