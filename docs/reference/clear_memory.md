# Clear all per-user memory records

Clear all per-user memory records

## Usage

``` r
clear_memory()
```

## Value

Invisibly `TRUE`.

## Examples

``` r
if (FALSE) { # \dontrun{
clear_memory()
} # }
```
